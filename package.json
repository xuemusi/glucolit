{
  "name": "glucolit",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "scripts": {
    "check": "node --check app.js && find functions -name '*.js' -print0 | xargs -0 -n1 node --check",
    "deploy": "wrangler pages deploy . --project-name=glucolit --branch=main"
  }
}
