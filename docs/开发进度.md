# 开发进度

## 2026-07-03

- 读取并整理 `项目方向GLUCOLIT-V3(1).pdf` 与前期黑客松适配评估内容。
- 明确本次黑客松 MVP 范围：1 天内做 H5 核心闭环，不做完整 APP。
- 输出团队共识文档：`docs/GLUCOLIT黑客松MVP范围共识.md`，覆盖产品定位、目标用户、必做模块、不做边界、AI 输出结构、合规话术、路演结构、团队分工和验收标准。
- 补充参考图功能取舍：明确图中首页、AI 工具、记录、同行、科普、安全说明等内容哪些纳入 MVP，哪些只放完整产品宣讲，哪些不采纳及原因。
- 调研 AI 电话实现方案，输出 `docs/AI电话实现方案调研.md`：建议本次采用 H5 内 AI 来电/实时语音 Agent + 预置语音兜底，不做真实手机号外呼；同步更新 MVP 文档的 AI 电话范围。
- 根据 `docs/面向糖尿病前期群体的AI健康管理H5产品需求讨论-2026年07月03日-来自【Get 笔记】.md` 更新 MVP 共识：确认 3 个 P0 页面和 2 个 P1 页面；新增配料表识别、压力/精力记录、五类行动卡、脱敏真实用户数据优先、AI 陪伴可选且由用户主动发起。
- 基于 MVP 范围共识输出可指导开发的 PRD：`docs/GLUCOLIT黑客松MVP_PRD.md`，包含产品定义、用户场景、页面功能、数据结构、AI 输出规则、合规要求、开发拆解和验收标准。
- 初始化项目配置：创建 `.gitignore` 文件，配置通用的 Node.js 依赖、各 IDE 缓存、操作系统临时文件以及敏感环境配置文件。

## 2026-07-04

- 参考“小清新/晨雾疗愈”视觉方向重做 H5 首页与通用组件风格：从深色监控面板改为浅米白底、薄荷绿主调、植物压纹、水彩式健康卡片。
- 首页首屏调整为 `今日健康概览`：新增状态分环、记录摘要、柔和餐后趋势图和三项信号卡，保留原有 AI 监控、行动建议和指标数据逻辑。
- 全局 UI 统一更新：圆角、边框、按钮、底部导航、拍照分析入口、弹窗、科普卡片、行动卡片均切换到参考图一致的温和健康管理风格。
- `index.html` 更新字体与静态资源版本号，避免旧缓存继续加载上一版样式。
- 调整 `.gitignore`：取消忽略 `docs/开发进度.md`，确保阶段性开发记录可进入版本管理；新增忽略 `.playwright-cli/`，避免本地 UI 验证截图和快照污染工作区。
- 核查拍照识别慢的问题：当前 `POST /api/analyze` 通过 `functions/_lib/llm.js` 调用 `kimi-k2.6` 的 `/chat/completions`，请求体未显式设置 `thinking`/`reasoning`，按 Kimi K2.6 默认参数等价于开启思考模式；前后端均等待完整 JSON 响应，当前链路为非流式。
- 新增本地验证用 `.env.local` 模板，包含 `TOKENDANCE_API_KEY`、`TOKENDANCE_BASE_URL` 和 `TOKENDANCE_MODEL`，用于后续先验证关闭思考模式与流式输出再改系统代码。
- 将模型思考模式与流式输出开关纳入配置：`.env.local` 和 `wrangler.toml` 均新增 `TOKENDANCE_THINKING_ENABLED=false`、`TOKENDANCE_STREAM_ENABLED=true`，后续代码通过 Cloudflare Functions `env` 读取。
- 完成关闭思考模式与流式识别改造：`functions/_lib/llm.js` 请求体统一带 `thinking.type`，`/api/analyze` 在 `TOKENDANCE_STREAM_ENABLED=true` 时返回 SSE；前端读取 `meta/token/done` 事件并展示打字机效果，最终仍渲染结构化分析卡片并写入 D1。
- 本地验证通过：直连 Tokendance `/chat/completions` 返回 `text/event-stream` 且 `reasoningLength=0`；本地 Pages Functions 端口 `/api/analyze` 返回 SSE、`thinking=disabled`、`fallback=false`；Playwright 验证页面能显示流式打字机文本并在完成后展示模型分析卡片。
- 优化拍照报告和食物识别的交互：在 `app.js` 中去掉图片文件输入框的 `capture="environment"` 属性，使 H5 在移动端可同时支持“直接拍照”与“从相册选择照片”。
- 改造 AI 报告识别链路：报告图片不再直接交给 Kimi 一步生成结论，改为 `qwen3-vl-plus` 先做严格 OCR，后端规则计算 OGTT 阈值、HOMA-IR、胰岛素/C肽倍数和风险信号，再由 `kimi-k2.6` 基于结构化数据生成谨慎解读。
- 报告解读规则新增：1 小时血糖只作为餐后早期波动信号，不能判成糖前期；2 小时 OGTT、空腹血糖和 HbA1c 才进入常用阈值判断；胰岛素 pmol/L 到 uIU/mL 的换算以区间表达，避免伪精确。
- 前端报告结果展示新增关键发现、规则计算字段、解读、行动建议和就诊问题，避免只显示简单字段表。
- 生产环境验证通过：`https://glucolit.xuemusi.com` 已加载 `20260704-structured-report` 资源；使用真实 OGTT 截图调用 `/api/analyze` 返回 SSE，`fallback=false`，模型链路为 `qwen3-vl-plus OCR + kimi-k2.6`，HOMA-IR 输出 `2.79-3.23`，并正确标记 `2h OGTT=normal`、`1h glucose=high_post_load_signal_not_diagnostic`。
- 改造食物/餐盘分析链路：食物图片改为 `qwen3-vl-plus` 先做餐盘结构识别，后端规则计算主食、蛋白质、蔬菜、油脂/汤汁、共享餐桌和碳水风险，再由 `kimi-k2.6` 生成温和行动建议。
- 食物分析规则新增：不估算精确热量；多人共享餐桌必须提示按实际摄入量确认；建议围绕主食份量、进餐顺序、汤汁/油脂和饭后轻活动输出。
- 生产环境验证通过：`https://glucolit.xuemusi.com` 已加载 `20260704-structured-meal` 资源；使用真实餐盘图调用 `/api/analyze` 返回 SSE，`fallback=false`，模型链路为 `qwen3-vl-plus vision + kimi-k2.6`，识别到黑米饭、清炒菠菜、番茄炒蛋、丝瓜炒肉片，碳水风险输出为 `低`，并提示共享餐桌按实际摄入量确认。
- 接入 GI/GL/II 餐盘知识库：新增 `functions/_lib/nutrition-knowledge.js`，从用户提供的 `GI × GL 食物完整对照表` 中抽取常见主食、蔬菜、蛋白质、乳制品、水果和饮品条目；餐盘规则层会按识别食物匹配知识库，输出 GI、GL、胰岛素指数参考，并把高 GL、低 GL 但高 II 等信号传给 Kimi 生成建议。
- 生产环境验证通过：`https://glucolit.xuemusi.com` 已加载 `20260704-nutrition-kb` 资源；使用真实餐盘图调用 `/api/analyze` 返回 SSE，`fallback=false`，结果含 `nutrition_notes`，黑米饭匹配糙米饭参考 `GI 55 / GL 10 / II 62`，菠菜、番茄、鸡蛋、丝瓜均匹配低 GL/低 II 条目。
- 阅读用户提供的《我的健康档案（养生的起点）》Markdown，完成需求拆解并新增 `docs/MY_HEALTH_PROFILE_REQUIREMENTS.md`：覆盖顶部诊断/复查摘要、OGTT 五点曲线、HOMA-IR/Matsuda/IGI/DI/AUC、HbA1c、甲功、血脂肝酶、抗体、合并症、分型、维护期、防复发、复查计划和每日清单。
- 新增“我的”功能 MVP：底部导航将“陪伴”替换为“我的”；`functions/_lib/health-profile.js` 沉淀结构化健康档案种子数据；`GET /api/app-state` 返回 `healthProfile`；前端新增健康档案摘要、核心指标趋势、OGTT 曲线、固定指标档案、维护期优先级、复查计划、行动清单和最近报告抽取展示。
- 更新技术与测试文档：`docs/TECHNICAL_PLAN.md` 记录健康档案数据流和后续 D1 表建议；`docs/TEST_CASES.md` 新增“我的”页测试用例并调整底部导航验收项。
- 优化拍照识别页交互：上传图片后在扫描框内展示缩略图和文件名，避免只显示文件名；流式识别期间改为局部更新进度卡片，不再反复重绘上传预览区；技术状态文案替换为面向 C 端用户的“正在读取/正在整理建议/建议已生成”等表达。
- 修复报告分析结果排版与专业度问题：新增 `docs/REPORT_ANALYSIS_LAYOUT_AND_MODEL_EVALUATION.md` 记录根因、模型测试和调整决策；报告后端新增 `standard_comparison`、`curve_rows`、`derived_indicators`、`professional_advice` 结构；前端改为标准对比表、曲线结构表、计算指标卡、专业建议分区，原始 OCR 字段默认折叠。
- 测试用户提供的 `88996api.cloud` 模型源：`gpt-5.4-mini` 请求超时，`gemini-2.5-pro-nothinking` 对同一份 OGTT 结构化数据输出更清晰；代码新增 `REPORT_NARRATIVE_*` 配置，生产可用 `gemini-2.5-pro-nothinking` 承担报告叙述，API Key 通过 Cloudflare secret 配置，不进入仓库。
- 更新前端静态资源版本到 `20260704-report-v2`，避免生产环境继续加载旧版报告排版。
- 完成当前 H5 页面内容与 C 端文案扫描：重点识别“我的”页的后台化字段表达、Demo/黑客松痕迹、医学术语堆叠、状态标签、科普内容来源表达和报告分析结果页的可理解性问题，形成待改文案清单。
- 新增 `docs/H5_C端文案复查记录.md`，记录本轮 C 端文案问题、优先级、修改范围和后续复查点；同步执行文案整改，移除主流程 Demo/黑客松痕迹，替换“后续要沉淀的字段”等后台话术，优化首页提醒、AI 工具、行动页、“我的”健康档案和科普页表达。
- 根据移动端截图继续修正报告页：分析完成后隐藏流式输出卡，避免“正在提取...”卡片停留在结果上方；关键发现从橙色左边条卡片改为轻量圆点列表；保留 `标准对比` 表格样式不变；静态资源版本更新为 `20260704-report-v3`。
- 解决与 C 端文案整改提交的 rebase 冲突后，将前端静态资源版本更新为 `20260704-report-v4`，确保报告页状态清理与文案整改同时生效。
- 优化餐盘/食谱分析结果视图：仅调整食物分析结果展示，把纯文字段落改为餐盘结构卡、重点信号卡、GI/GL/II 指标卡和步骤化建议；对“汤汁、油脂、拌饭、偏高、高 GL/II”等风险词做红色重点标记，静态资源版本更新为 `20260704-meal-visual-v1`。
- 继续修正餐盘结果视图：`建议吃法` 默认加入 `211 餐盘法（2 份蔬菜、1 份蛋白质、1 份主食）`；GI/GL 卡片按食物分组，避免 `番茄炒蛋≈番茄`、`番茄炒蛋≈鸡蛋` 重复标题；第三个指标由 `II` 改为 `营养素`，静态资源版本更新为 `20260704-meal-visual-v2`。
- 根据移动端截图继续调整餐盘结果视图：`重点信号` 和 `GI / GL / 营养素参考` 由卡片改为表格呈现，保留红色重点标记、211 餐盘法解释、食物分组去重和 `营养素` 字段，静态资源版本更新为 `20260704-meal-table-v1`。
- 完成线上产品体验巡检：使用账号 `19928786380` 访问 `https://glucolit.xuemusi.com/`，覆盖首页、AI、行动、我的、科普和移动端/桌面端展示；输出 `docs/GLUCOLIT线上产品体验报告-2026-07-04.md`，记录叙事逻辑、对客友好度、信息层级、AI 输出图表化、状态串台和页面优化建议。
- 复核 `feat: improve meal analysis visuals` 线上变更：确认生产环境已加载 `20260704-meal-visual-v1`；餐盘样例已能稳定展示餐盘结构卡、重点信号和替换建议，需更正上一轮“餐盘样例卡住/正文串台”的判断；同时确认 AI 工具切换后仍有“已生成建议”状态标签残留，报告解读与配料表分析仍需继续图表化，已补充到 `docs/GLUCOLIT线上产品体验报告-2026-07-04.md`。
- 按产品预期修正 AI 工具 tab 状态模型：报告、餐盘、配料表切换时不清空各自已生成数据，改为按 tab 分槽保存结果、上传预览、流式状态和模型 meta；“标签残留”定义为扫描面板右上角状态胶囊读到上一工具完成状态，已改为只读取当前 tab 状态。
- 更新前端静态资源版本到 `20260704-tool-tab-state-v1`；本地 Pages 环境验证通过：报告、餐盘、配料表样例结果在 tab 间切换后均可保留，未生成结果的 tab 显示“可以上传”，不会继承其他 tab 的状态标签。
- 在 `docs/GLUCOLIT线上产品体验报告-2026-07-04.md` 的需求池优化清单中，将第 1 项“AI tab 状态标签残留”标记为“已优化”，并补充验证版本 `20260704-tool-tab-state-v1`。
- 修复餐盘表格版线上未生效问题：上一轮代码已包含表格渲染和去重清洗，但后续提交把 `index.html` 的静态资源版本回退到 `20260704-v2`，导致浏览器继续加载旧卡片脚本；本轮恢复并升级为 `20260704-meal-table-v2`，确保生产环境加载表格版代码。
- 优化配料表与“我的”页信息层级：配料表分析结果新增三档红绿灯购买建议、触发原因卡和替代选择列表；“我的”页默认只展示顶部变化结论、关键 4 项和 OGTT 曲线，身体成分、完整复查指标、报告抽取、日常观察目标和时间线统一折叠到“完整专业指标”；静态资源版本更新为 `20260704-action-label-profile-v1`。
- 优化行动页信息架构：将 `0/5` 同权重清单改为 `1 个主行动 + 4 个可选行动`，主行动优先展示饭后步行；每条行动补充触发信号、推荐理由、降级动作和完成反馈，静态资源版本更新为 `20260704-action-label-profile-v1`。
- 同步更新 `docs/GLUCOLIT线上产品体验报告-2026-07-04.md`：将需求池第 6、7 项标记为已优化，并补充行动页仍需后续接入 AI 识别结果动态生成行动。
- 优化线上产品体验报告需求池第 3-5 点：首页去掉“模拟设备数据”对客展示；今日提醒按不同信号输出不同影响和建议；最近记录改为类型、更新时间、标题和两行摘要卡片，详情进入对应 AI 分析页；静态资源版本更新为 `20260704-home-records-v1`。
- 改造配料表分析链路：新增 `docs/LABEL_ANALYSIS_MODEL_EVALUATION.md` 记录 Gemini 与 GPT 对同一配料表图片的对比；配料表图片改为 `qwen3-vl-plus` 先抽取配料顺序和营养成分，规则层判断添加糖、精制碳水、甜味剂、钠、蛋白质和膳食纤维，再由 `gpt-5.4-mini` 生成克制建议；前端在现有红绿灯基础上补充购买红绿灯表、配料/营养表、好在哪里、需要留意、怎么吃/喝和替代选择，静态资源版本更新为 `20260704-label-analysis-v2`。
